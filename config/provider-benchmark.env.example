# Optional free-only, user-provisioned Robinhood mainnet endpoints.
# Store actual values in config/provider-benchmark.env (directory 0700, file 0600).
BENCH_CHAINSTACK_HTTP=
BENCH_CHAINSTACK_WS=
BENCH_VALIDATION_HTTP=
BENCH_VALIDATION_WS=
BENCH_DWELLIR_HTTP=
BENCH_DWELLIR_WS=
